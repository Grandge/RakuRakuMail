# 次回セッション開始プロンプト

最終更新: 2026-09-01（M1 Step 8 実装完了。メイン環境で検証済み・同期済み）

以下をそのまま貼ってセッションを始めてください。

---

「らくらくメール」というWebアプリを開発しています。作業ディレクトリは `d:\workspace\らくらくメール` です。

## まず読んでほしい文書

`Documents\` 配下を次の順で読んでください。

1. `M1実装計画.md` — **最重要。** 進捗表・着手順・詰まりどころが全部ここにあります
2. `要件定義書.md` — 仕様の本体（画面・メール形式・データ構造・処理手順・開発計画）
3. `決定事項.md` — 確定した設計方針の一覧（D-01〜D-65）。判断に迷ったらこれを正とする
4. `要求定義書.md` — 元の要求（CON / FUN / SND / PER / DES / NFR）

判断の経緯を記録した質問事項（第1〜4回）は、内容が `決定事項.md` に集約されたためリポジトリからは外しました。原本は `Documents/秘匿/質問事項アーカイブ/` にあります（Git管理外）。

## いまどこまで進んでいるか

M1「認証＋1対1のテキスト送受信」の **Step 8 まで実装が終わっています**（実機確認は未了）。

**M1の完了条件（2つのGmailアカウント間で送受信でき、相手のGmailで普通のメールとして読める）は実機で満たしました。**

| Step | 内容 | 状態 |
| --- | --- | --- |
| 0〜7 | Cloud Console / 足場 / 認証 / 送信 / 取り込み / メイン画面 / スレッド継続 / エラー処理 | **完了**（実機確認済み） |
| 8 | 永続化（IndexedDB ＋ AES-GCM ＋ 取り出せない鍵） | **実装完了・実機確認待ち** |
| 9 | GitHub Pages への配信 | **仕掛かり**（ワークフローまで。リポジトリ側の設定が残り） |

テストは172件で全部通っています（`npm test`）。`npm run build` も通ります。

---

## 最初にやること

**リポジトリ側の設定4件が残っています。これが済むまで配信できません。順番に意味があります。**

| # | 作業 | URL |
| --- | --- | --- |
| 1 | Secret `VITE_GOOGLE_CLIENT_ID` を登録 | `https://github.com/Grandge/RakuRakuMail/settings/secrets/actions` |
| 2 | Pages の Source を「GitHub Actions」に | `https://github.com/Grandge/RakuRakuMail/settings/pages` |
| 3 | `Deploy to GitHub Pages` を Re-run | `https://github.com/Grandge/RakuRakuMail/actions` |
| 4 | OAuth 生成元に `https://grandge.github.io` を追加 | `https://console.cloud.google.com/auth/clients?project=175373053675` |

**1 を 2 より先に。** 逆にするとクライアントIDが空のまま配信され、ビルドは成功するのにログインだけ失敗する、原因の分かりにくい状態になります（`VITE_GOOGLE_CLIENT_ID=` でビルドしてもエラーにならないことを実測で確認済み）。

クライアントIDの値は `.env.local` か `Documents/秘匿/控えておくもの.md` にあります。

CI は一度 `configure-pages` で失敗していますが、これは Pages 未有効化によるもので、手前の `npm ci` / `npm test` / `npm run build` は成功しています。

### 実機確認はどこでやるか

**リモート作業中なら、Pages 配信を先に済ませてください。** `https://grandge.github.io/RakuRakuMail/` はどこからでも開けるので、そこで Step 8 の確認までできます。ローカルの `127.0.0.1:5173` は、このPCの前にいるときしか使えません。

Pages のオリジンは `127.0.0.1` とは別なので IndexedDB は空から始まります。相手の登録もやり直しになりますが、保存が本当に効いているかを見るには好都合です。ログインするGoogleアカウントは、テストユーザーに登録済みのもの（`Documents/秘匿/控えておくもの.md` 参照）を使ってください。

---

## Step 8 / 9 で実装済みの内容（2026-09-01）

- **Step 8 の実装を完了**
  - `src/store/deviceKey.ts` … 取り出せない CryptoKey を「無ければ作る」。保管庫（`KeyVault`）を渡す形にして IndexedDB から切り離してあります
  - `src/store/db.ts` … IndexedDB 本体。`keys`（CryptoKey）と `settings`（暗号化した `Account` / `Contact[]`）の2ストア。読めない・書けない・鍵が変わったのいずれでも例外にせず、空として扱います
  - `src/domain/panel.ts` … `panelsFromContacts()` と `accountFromProfile()` を追加
  - `src/ui/screens/MainScreen.tsx` … 起動時に保存済みの相手を先に出してから取り込む。相手が増減したら保存し直す
  - **ログアウトでは端末の保存データを消しません。** 消去は S-07 設定画面の「この端末のデータを消す」から確認画面を挟んで行う仕様です（6.2 / 5.4 / D-55）。`db.ts` の `clearAll()` は用意してありますが、M1 では設定画面が無いのでどこからも呼んでいません
- **Step 9 のワークフローを追加**（`.github/workflows/deploy.yml`）
- テストを9件追加（163 → 172）

### 確認してほしいこと（サブPCではできなかった分）

M1実装計画 Step 8 の確認4項目を、`npm run dev` →`http://127.0.0.1:5173/RakuRakuMail/` でお願いします。

1. 相手を登録 → ページ再読み込み → 相手が残っている
2. DevTools → Application → IndexedDB → `rakurakumail` → `settings` の値が読めないバイト列になっている
3. `keys` ストアのレコードが `CryptoKey` で、`extractable: false`
4. `rakurakumail` を削除して再読み込みしても、アプリが落ちずに初期状態から始まる
5. （追加）ログアウトしても `rakurakumail` の中身は残る（消えるのは仕様違反。6.2）

### 配信が通ったら

`https://grandge.github.io/RakuRakuMail/` でログイン・送受信・Android実機を確認します（M1実装計画 Step 9 の確認1〜5）。ここまで通れば M1 は完了です。

### 未処理の文書作業

`決定事項.md` の **D-55**「ログアウト時にこの端末のデータを消す機能を入れる」という文言が読み違いを招きました（サブPCの実装が一度「ログアウトすると必ず消える」になり、6.2 と 5.4 に反していました）。**D-55 に、要件定義書 6.2「設定画面に用意する」を参照する注記を足してください。**

### 判断が要るところ（そのままにしてあります）

- `MainScreen` の保存は `panels` が変わるたびに `Contact[]` を丸ごと書き直す実装です。M1 の件数なら問題ありませんが、M2 で件数が増えるなら差分にする余地があります
- 相手の削除機能はまだ無いので、一度保存した相手は消せません（要件にも無いため未実装のままです）

---

## 開発環境の起動

```
npm run dev
```

**`http://127.0.0.1:5173/RakuRakuMail/` を開いてください。**`localhost` ではありません。Google Cloud Console が `localhost` を生成元に登録できなかったため、`127.0.0.1` に固定しています。オリジンが違うと `origin_mismatch` になります。

`Port 5173 is already in use` で落ちたら、前回の Vite が残っています。

```powershell
Get-NetTCPConnection -LocalPort 5173 -State Listen | Select-Object OwningProcess
Stop-Process -Id <PID> -Force
```

## 実測で分かっていること（推測で上書きしないでください）

| 項目 | 結果 |
| --- | --- |
| `gmail.modify` に `messages.send` が含まれるか | **含まれる。** `gmail.send` は不要（D-65） |
| Gmail が独自ヘッダ（3.3）を保存するか | **保存する。** 5ヘッダすべて残存 |
| `sendAs` の読み取りが `gmail.modify` に含まれるか | **含まれる** |
| 自分の表示名 | `sendAs.displayName` → `userinfo.name` の順で自動取得（D-64） |
| 目印の無い返信を取り込めるか | **取り込める。** `threadId` で同じ会話に入る（D-19 実証済み） |

## 設計上の約束（崩さないでください）

```
ui/  ──▶  domain/  ──▶  gmail/  ──▶  auth/
              │            │
              ▼            ▼
           store/       mail/
```

- `mail/` は **Gmail API を一切知りません。** 文字列とバイト列だけを扱う純粋関数にして単体テストで固めています
- `gmail/` は Gmail API の形だけを知り、らくらくメールの都合（目印・独自ヘッダ）を知りません
- `ui/` から `fetch` を書かないでください
- トークンは `auth/session.ts` のメモリ上のみ。`localStorage` にも IndexedDB にも書きません（D-54）
- 本文は `dangerouslySetInnerHTML` を使わず、テキストとして表示します（D-23 / 6.2）

## 秘匿情報の扱い

- `Documents/秘匿/` と `.env.local` は `.gitignore` 済みです
- **クライアントIDやメールアドレスを、Git管理下の文書やソースに書かないでください**（公開リポジトリのため）
- クライアントシークレットは使いません

## 制約のリマインド

- サーバーレス（GitHub Pages のみ）／ React + TypeScript + Vite ／ PWA化は M8
- 動作保証は Chrome・Edge（Windows PC）と Chrome（Android）。Safari はベストエフォート
- UIは日本語のみ。高齢者やITが苦手な人が使う前提で、専門用語を画面に出さない（5.4）
- 開発体制は私とあなたの2人。目標時期の設定なし。品質優先
- 文書を更新したら `決定事項.md` と `要件定義書.md` の整合を保ってください

## 進め方

大きめの変更に入る前は、まず方針を提示して確認を取ってください。実装後は、私がブラウザで確認できる形（確認項目の一覧）を出してください。

## 環境のメモ

- **`gh` のアクティブアカウントは `Grandge` です。** このPCには Grandge / Yasabihhagure / NaoyukiSivaSI の3つがログインしており、以前は Yasabihhagure がアクティブで push が 403 になりました。他のリポジトリで別アカウントを使う場合は `gh auth switch --user <名前>` で切り替えてください
- リポジトリは 2026-09-01 に作り直しました。それ以前の履歴（質問事項の原本を含む）はありません
- サブPC側にも作業コピーがあります。両方で同じファイルを触ると衝突するので、作業前に `git fetch` して差分を確認してください
